// Define the base API version for all endpoints
exports.API_VERSION = '/api/v1';

// Define the roles for authorization checks
exports.ROLES = {
    SUPERVISOR: 'supervisor',
    ENUMERATOR: 'enumerator',
};

// Define default settings for the application
exports.DEFAULT_PORT = 5000;